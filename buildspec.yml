version: 0.2

phases:
  build:
    commands:
      - echo Started build: `date`
      - python Adder_tst.py
  post_build:
    commands:
      - echo Finished build: `date`
artifacts:
  files:
    - Adder.py
